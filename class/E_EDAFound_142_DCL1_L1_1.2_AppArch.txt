Certainly! Below is the document with the embedded video HTML converted to the video's URL address:

---

**Summary:**
This video provides an overview of Vantiq application architecture by guiding viewers through the process of creating a Vantiq application. It covers the initial setup in a new namespace, the use of the Design Model tool to visualize and create applications, and the integration of services and clients. The video also demonstrates how to set up event schemas, log events, and display data using a number viewer widget. It explains the five basic components of Vantiq applications: services, clients, datastreams, external entities, and user data, emphasizing the integration capabilities of Vantiq with external systems.

**Original Content:**

filename: 142_DCL1_L1_1.2_AppArch  
video url: https://youtu.be/dBW2uznYGfY

Vantiq Application Architecture In this video we'll explore what comprises a Vantiq application by making one.

When we come to a new namespace, we're asked what kind of project we want.

By selecting the default design model, it will open up a tool that will help us both visualize and create an application.

We'll give this tool a name and a package and choose one of the options for how we want our design to go.

Packages are a scoping mechanism for grouping resources together, and for now we're just going to build our design from an empty template.

Before we start building, though, let's briefly introduce ourselves to the integrated development environment.

Next to the Vantiq logo is your project name, followed by the intuitive menu tabs for accessing Tools and Resources.

Look to the right for the name of your isolated workspace, called the Namespace.

Click the icons next to it to see your user information and set preferences.

Now back to our Design Model tool.

We have a small list of mostly services and clients to drag into our design.

We'll pull in one new Service and one new Client.

External is just a placeholder for outside entities.

To actually create a new service or client, I need to double click on it.

Give it a name designated as a new service or client and then save the design model and they will be generated by the system which you can verify by finding them listed in your project contents.

Also in the Design Model tool, by drawing a line from the Service to the Client, I can send events.

That way I'll save again and now the main pieces are in place for my application.

Let me right click on my service to bring it up and tweak a couple of things.

I'll click the + to add an incoming event with an event handler that will process them.

If I go to the interface tab, I'll see my outbound pathway too.

That happened when I drew that line to the Client.

While I'm here in the interface tab, I'm also going to set up the event schema.

That is the structure for the event as just a single integer in both the inbound and outbound events.

By doing this, I'm helping the tools automate more things for me, as I'll point out later.

So just to recap, my input event is a single integer, and my output event is a single integer.

Now, what am I going to do within this service to process that integer event?

Well, first I'm going to change the name of the event stream to the same name as the inbound event.

That's not required, it's just a personal preference.

Then we'll log the events that we see by bringing in a log stream event while we simultaneously send our number straight to the services outbound event.

This is a simple matter of picking our service and outbound event from the drop list.

I'm all done with the service, I'm saving it, and now I just need to make some events for it so that we can see how they work.

So I made some simulated events in Vantiq’s very handy event generator tool.

We start the generator, and immediately we can see events flowing into the system, and they're being logged as expected because we drew that line in the design model.

They're also being sent to the client.

Let's set up a display so we can see that.

I'll drag in a number viewer widget to the canvas, and when I select it, the configuration pane slides in from the right.

Here's where I can set up which data stream I'm going to use and which property of the data stream I want to display.

I'll start up the generator again, and when I do, the viewer displays a new number every second as the events come in.

Vantiq Applications are comprised of five basic components, most of which you just saw.

Let's go over each of them briefly.

Services encapsulate program logic and state they accept input, process it in some way, and push out the results as desired.

In our case, we created an inbound event to expect integers, then build a visual event handler to log them and publish them to our outbound event.

We'll be learning a lot of other ways these integers could have been processed, but for now it's important to just see the path that the events took inbound through a visual event handler, then published to the outbound.

Remember when we drew a line in our Design Model between our service and the client?

That created a pathway for events called a Datastream.

In the Client, we dragged in a number viewer widget to display what came through the Datastream.

Clients are the front end displays for applications.

In our demonstration, we used a Vantiq tool called an event generator to simulate events from an outside entity, But Vantiq is designed to integrate with just about any other system on the Internet.

Through the course of this class, we'll see several other examples of outside entities integrating with the Vantiq platform.

When we set up our inbound and outbound events, we also specified that the schema of the events would be as single integers.

Then when we dragged in the number viewer, it was easy to just select the integer property to display, No code required.

The one application piece you didn't see on our demonstration was user data, because we didn't need to persist our integer events.

If we did, we'd store them in standard types for later retrieval, updates, and deletions as needed.

The main application drivers are the services and clients.

Outside entities, database data and schema provide the materials and structure for services and clients to perform the application work.